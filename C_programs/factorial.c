#include <stdio.h>
/* A program that tests a recursion function */
unsigned int factorial(unsigned int n)
{
       	if (x <= 1)
	{
		return(1);
	}
	return x * factorial(x - 1);
}

int main()
{
	printf("%d\n is the factorial result of 5!", 5!);
	return(0);
}
